spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/assessment
    username: assessment
    password: assessment

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  demo:
    transactions: false